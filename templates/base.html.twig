<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;800&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
        {% block stylesheets %}{% endblock %}

        {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}

<style>
/* ==============================
      HERO (ENCABEZADO)
   ============================== */
.hero {
    position: relative;
    height: 180px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-image: url("{{ asset('imagenes/imagen_gimnasio.jpg') }}");
    background-size: cover;
    background-position: center;
    color: #fff;
    overflow: hidden;
}
.hero::before{
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.45);
}
.hero-title {
    position: relative;
    z-index: 2;
    font-size: 2.25rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 1px;
    text-shadow: 0 2px 8px rgba(0,0,0,0.6);
    font-family: 'Poppins', system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    margin: 0;
}

header.hero { margin: 0; }
nav.navbar { margin: 0; }

body { padding-top: 0; }
footer { font-size: .9rem; }

.nav-link.active {
    background: rgba(255,255,255,0.06);
    border-radius: .25rem;
}

@media (max-width: 576px) {
    .hero { height: 140px; }
    .hero-title { font-size: 1.75rem; }
}

/* ==============================================
      FONDOS DINÁMICOS POR SECCIÓN (SUAVES SIN SALTO)
   ============================================== */

body::before {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -1;
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    opacity: 1;
    transition: opacity 0.5s ease-in-out;
}

{% set route = app.request.attributes.get('_route') %}

{% if route starts with 'app_usuario' %}
    body {
        background: url("{{ asset('imagenes/usuarios.jpg') }}") no-repeat center center fixed;
        background-size: cover;
    }
{% elseif route starts with 'app_clase' %}
    body {
        background: url("{{ asset('imagenes/clases.jpg') }}") no-repeat center center fixed;
        background-size: cover;
    }
{% elseif route starts with 'app_reserva' %}
    body {
        background: url("{{ asset('imagenes/reservas.jpg') }}") no-repeat center center fixed;
        background-size: cover;
    }
{% elseif route starts with 'app_rutina' %}
    body {
        background: url("{{ asset('imagenes/rutinas.jpg') }}") no-repeat center center fixed;
        background-size: cover;
    }
{% elseif route starts with 'app_ejercicio' %}
    body {
        background: url("{{ asset('imagenes/ejercicios.jpg') }}") no-repeat center center fixed;
        background-size: cover;
    }
{% endif %}

/* ==============================
      TABLAS
   ============================== */
.table {
    background: #ffffff !important;
}
.table * {
    color: #000 !important;
}

.table-container,
.table {
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 4px 25px rgba(0,0,0,0.25);
}

/* ==============================
      BOTONES DE ACCIÓN
   ============================== */
.btn-icon {
    border-radius: 8px !important;
    font-weight: bold;
    padding: 4px 8px;
}

.btn-icon.view {
    background-color: rgba(0, 123, 255, 0.85) !important;
    color: white !important;
}

.btn-icon.edit {
    background-color: rgba(255, 193, 7, 0.85) !important;
    color: black !important;
}

.btn-icon.delete {
    background-color: rgba(220, 53, 69, 0.85) !important;
    color: white !important;
}

.btn-icon:hover {
    filter: brightness(1.25);
    transform: scale(1.05);
}

/* ==============================
      PAGINACIÓN
   ============================== */
.pagination li a,
.pagination li span {
    background: rgba(255,255,255,0.92) !important;
    color: #000 !important;
    border: 1px solid #ccc !important;
    font-weight: 600;
}

.pagination .active span {
    background: #0d6efd !important;
    color: #fff !important;
    border-color: #0d6efd !important;
}

.pagination li a:hover {
    background: #e9ecef !important;
}

/* ==============================
      FOOTER
   ============================== */
footer {
    background: rgba(0, 0, 0, 0.55) !important;
    backdrop-filter: blur(4px);
    border-top: 1px solid rgba(255,255,255,0.2);
}
footer small.text-muted {
    color: #ffffff !important;
}

/* ==============================
      FORMULARIOS (NUEVO / EDITAR / VER)
   ============================== */
/* ==== FORMULARIOS: NUEVO / EDITAR / VER ==== */
.card {
    background: #f8f9fa !important; /* blanco suave */
    color: #000 !important;
    border: 1px solid #dcdcdc !important;
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: 0 3px 15px rgba(0,0,0,0.10); /* sombra suave */
}

/* Inputs con blanco suave */
.card input,
.card select,
.card textarea {
    background: #ffffff !important; /* blanco normal */
    color: #000 !important;
    border: 1px solid #cfcfcf !important;
}

/* Labels más visibles */
.card label {
    color: #444 !important;
}


/* ==============================
      TEXTOS DE LA ZONA SUPERIOR (FILTROS + CABECERAS)
   ============================== */
/* Textos blancos SOLO para páginas con tablas y filtros */
body:not(.form-page) .container label,
body:not(.form-page) .container .form-label,
body:not(.form-page) .container .text-muted,
body:not(.form-page) .container h1,
body:not(.form-page) .container h2 {
    color: #fffffffb !important;
    text-shadow: 0 1px 3px rgba(0,0,0,0.8);
}



</style>


    </head>

    <body>
        <header class="hero">
            <h1 class="hero-title">GYMBRO</h1>
        </header>

        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand me-3" href="{{ path('app_portada') }}">GymBro</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="mainNavbar">
                    {% set current = app.request.attributes.get('_route') %}

                    <ul class="navbar-nav d-lg-none mb-2">
                      {% if app.user %}
                        <li class="nav-item dropdown">
                          <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle me-2"></i>
                            {{ app.user.nombre ? app.user.nombre : app.user.email }}
                          </a>
                          <ul class="dropdown-menu">
                            <li class="dropdown-header">{{ app.user.email }}</li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                              <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                <i class="bi bi-box-arrow-right me-2"></i> Cerrar sesión
                              </a>
                            </li>
                          </ul>
                        </li>
                      {% endif %}
                    </ul>

                    {% if app.user %}
                      <ul class="navbar-nav me-auto">
                        {% if is_granted('ROLE_ADMIN') %}
                          <li class="nav-item">
                            <a class="nav-link {{ current starts with 'app_usuario' ? 'active' }}" href="{{ path('app_usuario') }}">Usuarios</a>
                          </li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PROFESOR') %}
                          <li class="nav-item">
                            <a class="nav-link {{ current starts with 'app_clase' ? 'active' }}" href="{{ path('app_clase') }}">Clases</a>
                          </li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PROFESOR') or is_granted('ROLE_ALUMNO') %}
                          <li class="nav-item">
                            <a class="nav-link {{ current starts with 'app_reserva' ? 'active' }}" href="{{ path('app_reserva') }}">Reservas</a>
                          </li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PROFESOR') or is_granted('ROLE_ALUMNO') %}
                          <li class="nav-item">
                            <a class="nav-link {{ current starts with 'app_rutina' ? 'active' }}" href="{{ path('app_rutina') }}">Rutinas</a>
                          </li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PROFESOR') %}
                          <li class="nav-item">
                            <a class="nav-link {{ current starts with 'app_ejercicio' ? 'active' }}" href="{{ path('app_ejercicio') }}">Ejercicios</a>
                          </li>
                        {% endif %}
                      </ul>
                    {% endif %}

                    <ul class="navbar-nav ms-auto d-none d-lg-flex">
                      {% if app.user %}
                        <li class="nav-item dropdown">
                          <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle me-2"></i>
                            {{ app.user.nombre ? app.user.nombre : app.user.email }}
                          </a>
                          <ul class="dropdown-menu dropdown-menu-end">
                            <li class="dropdown-header">{{ app.user.email }}</li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                              <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                <i class="bi bi-box-arrow-right me-2"></i> Cerrar sesión
                              </a>
                            </li>
                          </ul>
                        </li>
                      {% else %}
                        <li class="nav-item">
                          <a class="btn btn-outline-light ms-lg-3" href="{{ path('app_login') }}">Iniciar sesión</a>
                        </li>
                      {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        <!-- WRAPPER DEL CONTENIDO (AÑADIDO) -->
        <div class="background-overlay">
            <div class="container mt-4">

                <div class="mb-3">
                    {% block back_button %}{% endblock %}
                </div>

                {% block body %}{% endblock %}
            </div>
        </div>

        <footer class="mt-5 py-3">
            <div class="container text-center">
                <small class="text-muted">© 2025 GymBro</small>
            </div>
        </footer>
    </body>
</html>
